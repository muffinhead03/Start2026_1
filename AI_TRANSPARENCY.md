# AI 투명성 리포트

> 2026-1학기 캡스톤 디자인과 창업 프로젝트 — **Do Not Open This Box.**  
> 팀 규교굥 (Team 5) | 이화여자대학교 컴퓨터공학과

---

## 1. AI 도구 활용 내역

| AI 도구 | 활용 목적 | 활용 결과물 | 인간 검수 여부 |
|--------|----------|------------|--------------|
| Claude (Anthropic) | 힌트 시스템 스크립트 초안 작성 | `HintManager.cs`, `HintEngine.cs`, `PromptBuilder.cs`, `OllamaClient.cs`, `PlayerState.cs`, `PuzzleConfig.cs`, `OrganRoomData.cs`, `WineGlassRoomData.cs`, `CLAUDE.md`, 작업 로그 | ✅ 팀원이 직접 검토 후 수정 |
| ChatGPT (OpenAI) | 기획 보조, 문서 초안 작성 | 기획 문서 초안, 발표 자료 초안 | ✅ 팀원이 직접 검토 후 수정 |
| Ollama (gemma3:4b) | 게임 내 플레이어 맞춤형 힌트 실시간 생성 | 게임 런타임 힌트 텍스트 | ✅ 프롬프트 설계 및 결과 검증 팀원이 수행 |
| Blender AI 기능 | 일부 3D 에셋 생성 보조 | 일부 텍스처 및 모델 초안 | ✅ 팀원이 직접 수정 및 재작업 |

---

## 2. AI 제안을 수정하거나 거부한 사례

### 사례 1 — `HintEngine.cs` 가중치 구조 변경
Claude가 초안으로 제안한 힌트 레벨 계산 로직은 단순 선형 합산 방식이었습니다.  
실제 플레이 테스트 후, 체류 시간과 실패 횟수가 과도하게 반영되어 힌트가 너무 이르게 강화되는 문제가 있었습니다.  
이를 팀원이 직접 감정(35%) / 요청방식(25%) / 정체(20%) / 진행률(15%) / 반복조사(5%) 의 가중치 구조로 재설계했습니다.

### 사례 2 — `PromptBuilder.cs` 씬 컨텍스트 서술 방식 변경
AI가 생성한 씬 컨텍스트 설명이 지나치게 추상적이어서 LLM이 구체적인 오브젝트 기반 힌트를 생성하지 못했습니다.  
팀원이 직접 각 씬의 오브젝트, 퍼즐 단계, 탈출 조건을 명세 수준으로 재작성했습니다.  
(예: `"Pipe 3 is in Room 3. Pipe 4 appears when the green LP number 7 is inserted into the LP player in Room 3."`)

### 사례 3 — `OllamaClient.cs` JSON 파싱 방식
Claude는 초안에서 Unity의 `JsonUtility`를 사용하는 방식을 제안했습니다.  
그러나 Ollama API 응답 구조에서 `JsonUtility`가 중첩 필드를 파싱하지 못하는 문제를 팀원이 직접 발견했습니다.  
이를 수동 문자열 파싱 방식(`"response":"..."` 필드 직접 추출)으로 대체했습니다.

### 사례 4 — gemma3:1b 모델 채택 거부
초기 실험에서 AI는 경량화를 이유로 gemma3:1b 사용을 권장했습니다.  
그러나 직접 테스트 결과 한국어 유지 실패, 문장 수 제한 미준수, hintLevel 반영 미흡 등의 문제가 확인되어  
팀원이 직접 gemma3:4b로 변경을 결정했습니다. ([실험 결과 참조](experiments/ollama-experiments/README.md))

---

## 3. AI 출력을 신뢰하기 어려웠던 순간

### 순간 1 — 한국어 응답 유지 불안정
gemma3 모델이 한국어 힌트 뒤에 영어 번역을 `(...)` 형태로 자동 추가하는 현상이 반복적으로 발생했습니다.  
시스템 프롬프트에 `"CRITICAL: You MUST respond in Korean language ONLY"` 등의 강제 지시를 추가하고,  
`OllamaClient.ParseResponse()`에서 괄호 이전까지만 잘라내는 후처리 로직을 팀원이 직접 구현했습니다.

### 순간 2 — hintLevel 무시
모델이 `hintLevel`에 관계없이 비슷한 강도의 힌트를 반복 생성하는 경우가 있었습니다.  
프롬프트에 레벨별 명시적 가이드(`LevelGuide` 딕셔너리)와 상태별 가이드(`StatusGuide`)를 추가하여 해결했습니다.

### 순간 3 — 코드 블록 삽입
모델이 간헐적으로 응답 내에 ` ``` ` 코드 블록을 삽입하는 경우가 있었습니다.  
게임 내 UI에 코드 블록이 그대로 출력되는 문제를 팀원이 발견하고, 파싱 로직에서 해당 패턴을 제거하도록 수정했습니다.

---

## 4. AI 없이 직접 수행한 작업

| 작업 영역 | 내용 |
|----------|------|
| Blender 3D 에셋 제작 | 게임 내 오브젝트 3D 모델링 전량 팀원 직접 제작 |
| Unity 씬 설계 및 배치 | 각 씬의 레벨 레이아웃, 오브젝트 배치, 라이팅 설정 |
| 퍼즐 설계 | 각 스테이지의 기믹, 단서 배치, 정답 흐름 설계 |
| 레벨 디자인 | 플레이어 동선, 난이도 곡선, 공포 연출 타이밍 |
| Ollama + gemma3:4b 로컬 테스트 | 모델 응답 직접 확인 및 품질 검증 |
| 발표 스크립트 및 슬라이드 | 팀원이 직접 작성 및 제작 |
| 멘토 상담 | 게임 개발 경험이 있는 멘토와 대면 상담 진행 |

---

> AI 출력물은 모두 팀원이 직접 검토하고 수정했습니다.  
> AI는 초안 생성과 실험 보조 도구로 활용되었으며,  
> 핵심 설계 결정, 퍼즐 구조, 레벨 디자인은 팀원이 주도했습니다.
